[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
pythonpath = [
  "runtime/python/src",
  "examples/demo/generated/python",
]
markers = [
  "load: heavy communication pressure profile (opt-in via ./scripts/pressure)",
]
