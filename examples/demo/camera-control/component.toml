[component]
name = "camera-control"
language = "cpp"

publishes = [
  "camera.v1.PoseChanged",
]

subscribes = []

calls = []

provides = [
  "camera.v1.CameraControl.SetPose",
]

[build]
adapter = "cmake"

[build.cmake]
source_dir = "."
target = "camera-control"

[artifact]
kind = "executable"
path = "camera-control"

[requirements]
libraries = ["libstdc++.so.6"]
