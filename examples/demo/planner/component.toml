[component]
name = "planner"
language = "go"

publishes = []

subscribes = [
  "tracking.v1.PersonTracked",
]

calls = [
  "camera.v1.CameraControl.SetPose",
]

provides = []

[build]
adapter = "go"

[build.go]
module_dir = "../../.."
package = "./examples/demo/planner"
output = "planner"

[artifact]
kind = "executable"
path = "planner"
